using System;
using System.Linq;

namespace FourPixCam
{
    //internal class ProcessingNet : NeuralNet
    //{
    //    #region ctor & fields

    //    NeuralNet _net;
    //    Layer[] _layers;

    //    internal ProcessingNet(NeuralNet net)
    //        :base(null)
    //    {
    //        _net = net ??
    //            throw new NullReferenceException($"{typeof(NeuralNet).Name} {nameof(net)} " +
    //            $"({GetType().Name}.ctor)");
    //        _layers = net.Layers.ToArray();
    //    }

    //    #endregion
    //}
}
